Выполненое тестовое задание.


Подключил свой стиль.
Sql запрсы реализованы через Entity, над оптимизацией запросов осбо не старался, 
поэтому отправка большого пакета данных много времени.

Параметры файла считываются, как то особо заморачиваться честно не стал ибо времени свободного на этой неделе особо не осталось.
Реализовал паттерн MVVM и разграничл модули и сервисы.

Есть вероятность что таблица сама в БД не создастся, в таком случе ниже прилогаю sql скрипт с таблицей (Проверять времени нет).

// самая простая табличка (БД postgre)
Create table Parameters
(
paramId serial primary key,
speed_2_1000 real,
speed_10_1000 real,
accel_2_1000 real,
accel_10_1000 real,
movement_2_1000 real,
movement_10_1000 real
);
Параметры подключения (Корректные) надо в начале запуска задать.
Так как прога работает с файлами лучше запускать от имени администратора, для того что бы винда не ругалась)


